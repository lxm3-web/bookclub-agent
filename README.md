# bookclub-agent — 讀書會製作助理（Agent 版）

claude.ai/code 選這個 repo（或本機 `claude`）→ 說「主持人的指示在 inbox，備一場」→ 訪綱、流程表、給作者的信、社群預告寫到 `outbox/` → 看完說「好，發下去」。

書《慢決策》、作者、出版社、節目皆虛構。**demo 完歸零**：`inbox/*.done` 改回 `.txt`、清 `outbox/`、log 只留表頭。
